MEMORY {
    RAM: start = $0000, size = $8000, type = rw, fill = yes;
    ROM: start = $8000, size = $7FFA, type = ro, fill = yes;
    VECTOR: start = $FFFA, size = 6, type = ro, fill = yes;
}

SEGMENTS {
    CODE: load = "RAM", type = rw;
    VECTOR: load = "VECTOR", type = ro;
}